steps:
  - label: Hello World!
    agents:
      queue: foo-bar-k8s
    plugins:
      - kubernetes:
          gitEnvFrom:
            - secretRef:
                name: buildkite-agent-stack-ssh-secret
          podSpec:
            serviceAccountName: buildkite
            containers:
              # - image: google/cloud-sdk:latest
              - command: [sh, -c]
                args:
                  - "'echo hello'"
                  # - "'gcloud storage ls'"
